// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
module.exports = {
    images: {
        domains: ['www.citypng.com', 'cdn.iconscout.com'],
    },
    reactStrictMode: true,
    swcMinify: true,
};